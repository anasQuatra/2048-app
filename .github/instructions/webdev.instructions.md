---
applyTo: '**'
---
Always use pnpm as my package manager. And always use TypeScript for all JavaScript projects.

---
applyTo: '.github/workflows/*.yml'
---
Always make sure to use the latest versions of GitHub Actions in my workflow files. For example, use `actions/checkout@v6` instead of older versions.