## notes

# après pnpm install, on a accès aux dépendences par ex: la commande pnpm nuxt typecheck
# pnpm nuxt typecheck remnote une erreur:
    <GameScoreDisplay label1="Best" :value="bestScore" />
    au lieu de :
    <GameScoreDisplay label="Best" :value="bestScore" />